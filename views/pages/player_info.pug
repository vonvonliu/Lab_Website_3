
//- player-info.pug
include ../partials/header.pug
include ../partials/menu.pug

div.container
  br
  div.row
    div.col-4
      div.card
        //- TODO: Modify the pug code below to conditionally set the image_src
        - let image_src = '../../resources/img/helmet.jpg'

        img.card-img-top#player_img(src= image_src, alt="Football Player")
        div.card-body.mx-auto
          form.form(action="/player_info/post", method="get")
            select.custom-select(name="player_choice", onchange="this.form.submit()")
              option(selected) Select Player
              for row in players
                  if(player_ID == row.id)
                    option(value=row.id selected)= row.name
                  else
                    option(value=row.id)= row.name
              //- TODO: Populate the select with player options. Set the value to the player's id and display the player's name.
    div.col-8
      div.table-responsive
        table.table
          //- TODO: Set the player's information. On select, fill in the player's information in each of the field below. You will need to calculate the average rushing, receiving, and passing yards.
          tr
            th Year
            if(player_ID)
              td= playerStats.year
            else
              td ?
          tr
            th Major
            if(player_ID)
              td= playerStats.major
            else
              td ?
          tr
            th Games Played
            if(player_ID)
              td= games
            else
              td ?
          tr
            if(player_ID)
              td= "Passing Yards: " + playerStats.passing_yards
            else
              td Passing Yards:
            if(player_ID)
              td= "Average Passing Yards: " + (playerStats.passing_yards / games).toFixed(2)
            else
              td Average Passing Yards:
          tr
            if(player_ID)
              td= "Rushing Yards: " + playerStats.rushing_yards
            else
              td Rushing Yards:
            if(player_ID)
              td= "Average Rushing Yards: " + (playerStats.rushing_yards / games).toFixed(2)
            else
              td Average Rushing Yards:
          tr
            if(player_ID)
              td= "Receiving Yards: " + playerStats.receiving_yards
            else
              td Receiving Yards:
            if(player_ID)
              td= "Average Receiving Yards: " + (playerStats.receiving_yards / games).toFixed(2)
            else
              td Average Receiving Yards:

include ../partials/footer.pug
